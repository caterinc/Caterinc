"use client";

// Meta Pixel removed — components kept as no-ops to avoid broken imports
export function MetaViewContent(_props: { productId: string; name: string; price: number }) {
  return null;
}

export function MetaAddToCart(_props: { productId: string; name: string; price: number }) {
  return null;
}
